#include <stdio.h>
#include <conio.h>
void main()
{
    char ch;
    printf("\n enter the character:");
    scanf("%c",&ch);
    if(ch=='a'||ch=='e'||ch=='i'||ch=='o'||ch=='u'){
    printf("\n enter the character is vowel");
}
    else{
        printf("\n the character is not vowel");
    }
    getch();
    }
